import 'package:flutter/material.dart';

class CreateArticleTab extends StatelessWidget {
  const CreateArticleTab({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset('assets/images/create_your_article.png'),
    );
  }
}
