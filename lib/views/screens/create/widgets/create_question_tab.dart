import 'package:flutter/material.dart';

class CreateQuestionTab extends StatelessWidget {
  const CreateQuestionTab({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset('assets/images/ask_your_question.png'),
    );
  }
}
